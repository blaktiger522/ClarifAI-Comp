name: clarifai_comp
description: ClarifAI - Handwriting to text conversion app using OCR
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'
  flutter: ">=3.10.0"

dependencies:
  flutter:
    sdk: flutter
  camera: ^0.10.5
  image_picker: ^1.0.4
  http: ^1.1.0
  flutter_secure_storage: ^8.0.0
  permission_handler: ^11.0.1
  flutter_spinkit: ^5.2.0
  image: ^4.0.17
  cupertino_icons: ^1.0.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
  assets: []
  fonts: []